All six multi SFS files are included as well as tpl and est files for one of the models as an example. 
The example models the south pre population, i.e., historical samples from the introduced range collected before 1957 in latitudes <35°, introduced from France with gene flow from a secondary source from Spain
All parameters and priors are constant for all models
